# Codex 懒人包 #02.5：连接 GitHub 与 Obsidian 第二大脑

> 版本：v0.4（Codex Desktop 版）
> 更新日期：2026-04-28
> 对应影片：Codex基本功 EP03（建议主题）

> 本篇是「Codex 版整合实作篇」。GitHub 与 Obsidian 的基础观念在 Claude 基本功系列已经讲过，本篇只聚焦 Codex 要完成的两件事：
>
> 1. 用 GitHub CLI 让 Codex 可以 `commit` / `push` 到 GitHub。
> 2. 找到使用者的 Obsidian vault，安装 MCPVault，让 Codex 可以读写第二大脑。

---

## 公开网址

- Codex 懒人包总入口：[https://github.com/mathruffian-dot/codex-lazy-packs](https://github.com/mathruffian-dot/codex-lazy-packs)
- 本篇公开网址：[https://github.com/mathruffian-dot/codex-lazy-packs/blob/master/02.5-%E9%80%A3%E6%8E%A5GitHub%E8%88%87Obsidian.md](https://github.com/mathruffian-dot/codex-lazy-packs/blob/master/02.5-%E9%80%A3%E6%8E%A5GitHub%E8%88%87Obsidian.md)

> [!tip]
> 影片说明栏建议放「本篇公开网址」。如果观众想看全部 Codex 懒人包，再引导他们回到总入口。

---

## 本篇和 Claude 基本功的关系

Claude 基本功里，GitHub 与 Obsidian 拆成多集讲：

| 主题 | Claude 版重点 | Codex 本篇怎么处理 |
|------|---------------|--------------------|
| GitHub | Git、GitHub CLI、登入、Pages、发布 | 只做 Codex 需要的 `gh auth`、`commit`、`push` 验证 |
| Obsidian | 安装 Obsidian、建立 vault、同步方案、MCP | 不重讲 Obsidian 教学，只找 vault 并设定 Codex MCP |

> [!tip]
> 如果你完全不知道 GitHub 是什么，或还没建立 Obsidian 笔记本，先回去看 Claude 基本功对应集数。本篇假设你大致知道 GitHub / Obsidian 是什么，只需要把它们接进 Codex。

---

## 这个懒人包会帮你做什么？

完成后，Codex 可以：

- 检查 Git / GitHub CLI 是否已安装
- 用网页端登入 GitHub CLI
- 设定 Git 使用者资讯
- 建立测试 repo，验证 Codex 可以 commit / push
- 找到既有 Obsidian vault
- 安装 `@bitbonsai/mcpvault`
- 写入 Codex MCP 设定
- 重启后验证 Codex 可以读写 Obsidian 笔记

---

## 先备条件

- [ ] 已安装 Codex Desktop app
- [ ] 已有 GitHub 帐号
- [ ] 已有 Obsidian vault，或知道自己的笔记本在哪里
- [ ] 电脑有网路
- [ ] Windows 使用者建议先完成 [[Codex 懒人包/01.5-Codex必装Skills与Plugins]] 的基础工具快筛

> [!warning]
> 本篇不是完整 Obsidian 入门教学。若使用者还没安装 Obsidian 或还没有 vault，请先让他照 Claude 基本功 Obsidian 集数建立笔记本，再回来跑本篇。

> [!important]
> GitHub 跟 GitHub Copilot 是不同服务。本篇只需要 GitHub 免费帐号、Git、GitHub CLI，不需要 GitHub Copilot Pro / Pro+ 订阅。近期 GitHub Copilot 个人方案改成 AI Credits 计费，影响的是 Copilot 这个 AI 写程式服务，不影响 Codex 使用 GitHub CLI 进行 `commit` / `push` 的流程。

---

## 整体流程

```text
阶段一：GitHub CLI
  1. 检查 Git / gh
  2. 网页端登入 gh
  3. 设定 Git 使用者
  4. 建立测试 repo，验证 commit / push

阶段二：Obsidian MCP
  1. 找到 Obsidian vault
  2. 检查 Node.js / npm
  3. 安装 mcpvault
  4. 写入 ~/.codex/config.toml
  5. 重启 Codex
  6. 测试读写笔记
```

---

# 阶段一：设定 GitHub CLI

## 步骤一：检查 Git 与 GitHub CLI

请 Codex 执行：

```powershell
git --version
gh --version
```

如果 Git 没有安装，Windows：

```powershell
winget install --id Git.Git --accept-source-agreements --accept-package-agreements
```

如果 GitHub CLI 没有安装，Windows：

```powershell
winget install --id GitHub.cli --accept-source-agreements --accept-package-agreements
```

安装后，请完全重启 Codex Desktop app，再重新检查。

> [!important]
> GitHub 外挂程式能让 Codex 读 GitHub；但要让 Codex 在本机 `commit` / `push`，仍然需要 Git 与 GitHub CLI。

---

## 步骤二：用网页端登入 GitHub CLI

先检查：

```powershell
gh auth status
```

如果尚未登入：

```powershell
gh auth login --web --git-protocol https
```

流程：

1. PowerShell 显示一次性验证码。
2. 浏览器开启 GitHub device login。
3. 若没自动开启，手动开：

```text
https://github.com/login/device
```

4. 输入验证码并授权。
5. 回到 Codex，重新执行：

```powershell
gh auth status
```

成功时应看到：

```text
Logged in to github.com account <你的帐号>
Git operations protocol: https
Token scopes: ... repo ...
```

---

## 步骤三：设定 Git 使用者资讯

检查：

```powershell
git config --global user.name
git config --global user.email
```

如果没有设定，请询问使用者姓名与 email：

```powershell
git config --global user.name "你的姓名"
git config --global user.email "你的email@example.com"
```

---

## 步骤四：建立测试 repo，验证 Codex 可以 commit / push

建立测试资料夹：

```powershell
New-Item -ItemType Directory -Path "$env:USERPROFILE\Documents\codex-github-test" -Force
Set-Location "$env:USERPROFILE\Documents\codex-github-test"
```

建立测试档：

```powershell
@"
# Codex GitHub 测试

如果这个档案成功出现在 GitHub，代表 Codex 可以 commit 与 push。
"@ | Set-Content -Encoding UTF8 -Path ".\README.md"
```

初始化并提交：

```powershell
git init
git add README.md
git commit -m "建立 Codex GitHub 测试"
```

建立 GitHub repo 并 push：

```powershell
gh repo create codex-github-test --private --source=. --push
```

验证：

```powershell
gh repo view codex-github-test --web
```

> [!warning]
> 若浏览器开启需要授权，请让使用者手动确认。若不想开浏览器，可改用 `gh repo view codex-github-test` 查看。

---

## 步骤五：测试 repo 要保留还是删除

测试成功后，请问使用者：

```text
GitHub 测试成功。这个 codex-github-test repo 要保留，还是删除？
```

若要删除 GitHub repo：

```powershell
gh repo delete codex-github-test --yes
```

若要删除本机测试资料夹：

```powershell
Remove-Item -LiteralPath "$env:USERPROFILE\Documents\codex-github-test" -Recurse -Force
```

> [!important]
> 删除 repo 或资料夹前一定要问使用者。

---

# 阶段二：设定 Obsidian MCPVault

## 步骤六：找到使用者的 Obsidian vault

先问使用者：

```text
你的 Obsidian 笔记本放在哪里？如果不知道，我可以帮你找。
```

常见位置：

| 同步方式 | 常见路径 |
|----------|----------|
| OneDrive | `C:\Users\<你>\OneDrive\文件\<vault名称>` |
| Google Drive | `G:\我的云端硬碟\<vault名称>` |
| Documents | `C:\Users\<你>\Documents\<vault名称>` |
| Obsidian Sync | 本机任意资料夹，只要里面有 `.obsidian` |

如果使用者不知道，Windows 可搜寻：

```powershell
$roots = @(
  "$env:USERPROFILE\OneDrive",
  "$env:USERPROFILE\Documents",
  "$env:USERPROFILE\Desktop",
  "G:\我的云端硬碟",
  "G:\My Drive"
)

$roots |
  Where-Object { Test-Path $_ } |
  ForEach-Object {
    Get-ChildItem -Path $_ -Recurse -Directory -Force -ErrorAction SilentlyContinue |
      Where-Object { Test-Path (Join-Path $_.FullName ".obsidian") } |
      Select-Object FullName
  }
```

找到候选 vault 后，请使用者选主要 vault，不要自行猜。

确认条件：

- 资料夹存在
- 里面有 `.obsidian`
- 使用者确认这是他的主要笔记本

把路径记成：

```text
<VAULT_PATH>
```

---

## 步骤七：检查 Node.js 与 npm

mcpvault 需要 Node.js / npm。

请执行：

```powershell
node --version
npm.cmd --version
```

如果没有 Node.js，Windows：

```powershell
winget install --id OpenJS.NodeJS --accept-source-agreements --accept-package-agreements
```

安装后重启 Codex Desktop app，再确认：

```powershell
node --version
npm.cmd --version
```

---

## 步骤八：安装 mcpvault

Windows PowerShell 建议用 `npm.cmd`，避免 PowerShell 执行原则挡到 `npm.ps1`。

```powershell
npm.cmd install -g @bitbonsai/mcpvault
```

确认安装位置：

```powershell
where.exe mcpvault
```

常见位置：

```text
C:\Users\<你>\AppData\Roaming\npm\mcpvault.cmd
```

把这个完整路径记成：

```text
<MCPVAULT_CMD>
```

---

## 步骤九：写入 Codex MCP 设定

Codex 设定档通常在：

```text
C:\Users\<你>\.codex\config.toml
```

请先备份：

```powershell
$config = "$env:USERPROFILE\.codex\config.toml"
$backup = "$config.bak-$(Get-Date -Format 'yyyyMMdd-HHmmss')"
Copy-Item -LiteralPath $config -Destination $backup -Force
```

加入或更新以下区块：

```toml
[mcp_servers.obsidian]
command = "<MCPVAULT_CMD>"
args = ["<VAULT_PATH>"]
startup_timeout_sec = 20
tool_timeout_sec = 60
```

Windows 范例：

```toml
[mcp_servers.obsidian]
command = "C:\\Users\\mathr\\AppData\\Roaming\\npm\\mcpvault.cmd"
args = ["C:\\Users\\mathr\\OneDrive\\文件\\Secondbrain"]
startup_timeout_sec = 20
tool_timeout_sec = 60
```

> [!warning]
> Windows TOML 路径要用双反斜线 `\\`。
> `args` 里的 vault 路径也要用 `\\`。

---

## 步骤十：手动测试 mcpvault

在重启 Codex 前，先测 mcpvault 本身能不能启动。

```powershell
'{"jsonrpc":"2.0","id":1,"method":"tools/list"}' |
  & "<MCPVAULT_CMD>" "<VAULT_PATH>"
```

如果成功，应该会看到工具清单，包含类似：

```text
read_note
write_note
search_notes
list_directory
get_vault_stats
```

若这一步失败，先不要重启 Codex，先排查路径。

---

## 步骤十一：重启 Codex 并验证 Obsidian MCP

请使用者：

1. 完全关闭 Codex Desktop app
2. 重新开启
3. 回到任一专案
4. 请 Codex 检查 Obsidian MCP 是否可用

测试方式：

```text
请列出我的 Obsidian vault 根目录资料夹。
```

再测写入：

```text
请在我的 Obsidian 笔记本建立 `Codex 测试/GitHub-Obsidian-连线测试.md`，内容写入今天日期、目前专案名称，以及「Codex 已成功连接 Obsidian MCP」。
```

最后读回：

```text
请读回刚刚建立的 Obsidian 测试笔记。
```

---

## 完成回报格式

请 Codex 完成后用这个格式回报：

```md
## GitHub / Obsidian 连接完成

### GitHub

- Git：已安装 / 已补装
- GitHub CLI：已安装 / 已补装
- gh 登入：成功 / 失败
- Git 使用者资讯：已设定 / 待设定
- commit / push 测试：成功 / 未执行
- 测试 repo：保留 / 已删除 / 未建立

### Obsidian

- Vault 路径：<VAULT_PATH>
- mcpvault：已安装 / 未安装
- Codex MCP 设定：已写入 / 未写入
- config.toml 备份：<备份路径>
- 重启 Codex：已完成 / 待使用者完成
- 读取测试：成功 / 失败
- 写入测试：成功 / 失败

### 下一步

1. 实际专案要发布时，可直接请 Codex commit / push。
2. 工作收尾时，把踩坑与进度写回 Obsidian。
3. 若要全域自动化，后续再设定开工 / 收工 skills。
```

---

## 踩坑笔记

| 状况 | 原因 | 解法 |
|------|------|------|
| `git` 找不到 | Git 未安装或 Codex 尚未重启 | 安装 Git 后重启 Codex |
| `gh auth status` 显示未登入 | GitHub CLI 尚未授权 | `gh auth login --web --git-protocol https` |
| `gh auth login --web` 在 Codex 里等到逾时 | GitHub 登入需要使用者到浏览器完成授权，Codex 无法代替输入帐密 | 开一个可互动 PowerShell 视窗执行 `gh auth login --web --clipboard --git-protocol https --hostname github.com`，使用者完成浏览器授权后再回来验证 |
| `gh auth status` 出现 `failed to read configuration` 或 `Access is denied` | Codex 沙盒读不到 `AppData\Roaming\GitHub CLI\config.yml` | 允许 Codex 读取 GitHub CLI 设定后重跑；这不一定代表 GitHub 尚未登入 |
| `gh --version` Access denied | Codex 读 GitHub CLI 设定被挡 | 允许 Codex 读取后重跑 |
| GitHub 外挂能读但不能 push | 外挂不等于本机 Git 权限 | 检查 `gh auth status` |
| Git 全域姓名 / email 是空的 | 只安装 Git 不会自动建立 commit 作者资讯 | 用 `git config --global user.name` 与 `git config --global user.email` 补上；不想公开信箱可用 GitHub no-reply email |
| `npm.ps1` 被挡 | PowerShell 执行原则 | 改用 `npm.cmd` |
| `npm.cmd install -g @bitbonsai/mcpvault` 出现 `EPERM` 或无法写入 npm cache | Codex 沙盒没有权限写入 `AppData\Local\npm-cache` 或全域 npm 目录 | 改用已授权 / 提权模式重跑同一个 npm 安装指令 |
| `where mcpvault` 找不到 | npm 全域路径不在 PATH | 到 `AppData\Roaming\npm` 找完整路径 |
| mcpvault 已安装但 `where.exe mcpvault` 仍找不到 | 目前的 Codex / PowerShell 程序还没刷新 PATH | 用 `npm.cmd prefix -g` 找全域路径，直接使用 `C:\Users\<你>\AppData\Roaming\npm\mcpvault.cmd` 测试 |
| TOML 路径失败 | Windows 反斜线没跳脱 | 使用 `C:\\Users\\...` |
| TOML 或终端机显示 `文件` 变成乱码 | 终端机编码显示问题，不一定代表档案路径真的坏掉 | 以实际 mcpvault 测试结果为准；若 `tools/list` 能回传工具清单，代表路径可用 |
| 改完 config 后工具没出现 | Codex 尚未重启 | 完全关闭再开 Codex |
| 找不到 vault | 路径错或不是 Obsidian vault | 搜寻 `.obsidian` 并请使用者确认 |
| 找到 OneDrive 与 Documents 两个相似 vault | 可能是同步资料夹与本机副本，内容看起来一样 | 优先选使用者实际同步中的 vault，例如 `OneDrive\文件\Secondbrain`，并确认里面有 `.obsidian` |
| 下载到本机的 Markdown 看起来是乱码 | 本机档案或读取工具的编码与 UTF-8 不一致 | 回到 GitHub repo 原始档确认内容，不要直接依乱码版本修改 |

---

## 本篇不做的事

- 不完整重讲 GitHub 是什么
- 不完整重讲 Obsidian 怎么用
- 不强迫新建 Obsidian vault
- 不强迫使用 Google Drive 或 OneDrive
- 不把 GitHub token 写进 `AGENTS.md`
- 不在未确认前删除测试 repo 或测试资料夹

---

## 相关懒人包

- [[Codex 懒人包/01.5-Codex必装Skills与Plugins]]
- [[Codex 懒人包/02-连接-GitHub]]
- [[Codex 懒人包/03-建立第二大脑-Obsidian]]
- [[Codex 懒人包/09-用chezmoi同步Codex设定]]

---

## 更新纪录

| 日期 | 版本 | 更新内容 |
|------|------|----------|
| 2026-04-28 | v0.4 | 补充 Codex Desktop 实测踩坑：GitHub 登入逾时、GitHub CLI 设定权限、npm 全域安装 EPERM、mcpvault PATH、vault 候选与编码显示问题 |
| 2026-04-28 | v0.3 | 补上 Codex 懒人包总入口与本篇公开网址 |
| 2026-04-28 | v0.2 | 补充 GitHub 与 GitHub Copilot 差异，确认不需 Copilot 订阅 |
| 2026-04-28 | v0.1 | 初版，整合 Codex GitHub CLI commit / push 与 Obsidian MCPVault 连接 |
