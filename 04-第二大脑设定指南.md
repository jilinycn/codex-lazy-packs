# Codex 懒人包 #04：第二大脑设定指南

> 版本：v0.2（Codex 版）
> 更新日期：2026-04-26
> 前置：建议先完成 #03，确认 Codex 已知道 `<VAULT_PATH>`，并已通过至少一种读写测试。

---

## 这个懒人包会帮你做什么？

把一个 Obsidian vault 整理成适合 Codex 长期协作的「第二大脑」：

- 建立三层资料夹：输入、消化、输出
- 建立全域与 vault 内两层 `AGENTS.md`
- 建立不依赖 Templater 的纯 Markdown 模板
- 建立 `知识库/index.md` 与 `知识库/log.md`
- 设定 Web Clipper 或手动剪藏流程
- 建立每周知识重整 SOP
- 保留踩坑笔记，让后面使用者避开同样问题

> [!important]
> 本懒人包预设「补缺，不覆盖」。
> 如果目标档案已存在，Codex 应先读取原档并询问使用者是否合并、跳过或备份后覆写。

---

## 先确认：你现在是哪一种状态？

| 状态 | 做法 |
|------|------|
| 已完成 #03 且 MCP 可用 | 可直接照本懒人包执行 |
| 已完成 #03 但只用资料夹授权 | 可执行，但要确认 Codex 对 `<VAULT_PATH>` 有写入权限 |
| 还不知道 vault 在哪 | 先回 #03 找到 `<VAULT_PATH>` |
| 已有成熟 vault | 务必使用「补缺，不覆盖」模式 |
| 全新空 vault | 可照本懒人包完整建立 |

本懒人包所有路径都用：

```text
<VAULT_PATH>
```

请把它替换成使用者实际的 Obsidian vault 路径。

---

## 阶段一：询问使用者偏好

Codex 先问使用者四件事：

1. 你的职业或主要身份是什么？
2. 你的笔记主要用途是什么？例如教学、研究、影片创作、专案管理。
3. 希望笔记与回复使用什么语言？预设繁体中文。
4. 这个 vault 是否已经有重要资料？如果有，预设不覆盖任何既有笔记。

建议记录成：

```markdown
## 使用者偏好

- 身份：<使用者回答>
- 主要用途：<使用者回答>
- 语言：繁体中文
- 覆写原则：不覆写既有笔记，先读原档再询问
```

---

## 阶段二：建立三层资料夹结构

在 `<VAULT_PATH>` 建立下列资料夹。已存在就跳过。

```text
<VAULT_PATH>/
├── Clippings/           # 输入：网路文章、影片笔记、外部资料
├── 知识库/              # 消化：AI 整理后的结构化知识
├── 创作库/              # 输出：自己的教案、教材、脚本、报告
├── 每日笔记/            # 时间管理：每日纪录、周计划、周报
├── Templates/           # 纯 Markdown 模板
└── AGENTS.md            # vault 内笔记规则
```

PowerShell 范例：

```powershell
$vault = "<VAULT_PATH>"
$folders = @("Clippings", "知识库", "创作库", "每日笔记", "Templates")
foreach ($folder in $folders) {
  New-Item -ItemType Directory -Force -Path (Join-Path $vault $folder) | Out-Null
}
```

资料夹语意：

| 层 | 资料夹 | 放什么 | 原则 |
|----|--------|--------|------|
| 输入 | `Clippings/` | 剪藏、逐字稿、外部资料 | 原始资料，少改写 |
| 消化 | `知识库/` | AI 整理后的主题页 | AI 写，人审 |
| 输出 | `创作库/` | 自己的作品 | 保留个人声音 |
| 时间 | `每日笔记/` | 每日、每周、重整周报 | 记录流程 |
| 模板 | `Templates/` | 笔记格式 | 不依赖外挂 |

---

## 阶段三：建立两层 AGENTS.md

### 3-1. 全域 AGENTS.md：让 Codex 记得 vault 在哪

位置：

```text
C:\Users\<使用者>\.codex\AGENTS.md
```

macOS / Linux：

```text
~/.codex/AGENTS.md
```

建议加入：

```markdown
## Obsidian 第二大脑

主要 Obsidian Vault：

`<VAULT_PATH>`

当我说「Obsidian」、「Secondbrain」、「我的笔记本」、「第二大脑」时，预设指这个资料夹。

如果任务涉及笔记、剪藏、知识库、创作库、每日笔记、专案工作流程，请优先读取：

- `<VAULT_PATH>\AGENTS.md`
- `<VAULT_PATH>\知识库\index.md`
- `<VAULT_PATH>\知识库\log.md`

实际读写仍以 Codex 工作区授权或 MCP 设定为准。
```

### 3-2. Vault 根 AGENTS.md：定义笔记规则

位置：

```text
<VAULT_PATH>\AGENTS.md
```

建议内容：

```markdown
# 我的第二大脑 — AGENTS.md

## 使用者身份

- 身份：<使用者身份>
- 主要用途：<教学 / 研究 / 创作 / 专案管理>
- 语言：繁体中文

## 资料夹规则

| 资料夹 | 用途 | 原则 |
|--------|------|------|
| `Clippings/` | 外部输入 | 原始资料，不任意改写 |
| `知识库/` | 结构化知识 | AI 可协助整理，使用者审核 |
| `创作库/` | 自己的作品 | 不改写个人声音，除非使用者要求 |
| `每日笔记/` | 时间纪录 | 记录每日、每周与重整结果 |
| `Templates/` | 模板 | 纯 Markdown，避免外挂依赖 |

## 新增笔记规则

- 正式笔记要加 frontmatter：`title`、`date`、`type`、`tags`
- 优先用 Obsidian 双向连结 `[[...]]`
- 新增知识库页面后，要更新 `知识库/index.md`
- 重要操作要追加到 `知识库/log.md`

## 安全规则

- 不覆写既有笔记；如果档案存在，先读原档再询问
- 不删除笔记，除非使用者明确指定
- 不批次改写个人风格强烈的文章
- 不把 API Key、token、密码写进笔记

## 常用指令语意

- 「新增到笔记」：存进本 vault，并自动判断资料夹
- 「整理成知识库」：从 Clippings 或输入内容建立 `知识库/` 页面
- 「跑一次知识重整」：执行每周知识重整 SOP
```

> [!tip]
> 全域 `AGENTS.md` 管「Codex 要去哪里找 vault」。
> Vault 根 `AGENTS.md` 管「这个 vault 里的笔记怎么写」。

---

## 阶段四：建立纯 Markdown 模板

本版预设不用 Obsidian Templater，避免外挂依赖与 `<% tp.xxx %>` 被误展开。

在 `Templates/` 建立三个模板。若档案已存在，先读原档再询问是否合并。

### 4-1. `Templates/每日笔记.md`

```markdown
---
title: 每日笔记
date: YYYY-MM-DD
type: daily-note
tags: [每日笔记]
---

# YYYY-MM-DD 每日笔记

## 今日重点

- （待填）

## 教学 / 工作纪录

- （待填）

## 收集到的材料

- （待填）

## 需要整理进知识库

- （待填）

## 明日优先事项

1. （待填）
2. （待填）
3. （待填）
```

### 4-2. `Templates/周计划.md`

```markdown
---
title: 周计划
week: YYYY-WXX
type: weekly-plan
tags: [周计划]
---

# YYYY-WXX 周计划

## 本周目标

- （待填）

## 本周重要事件

| 日期 | 事件 | 备注 |
|------|------|------|
| | | |

## 本周创作 / 专案

- （待填）

## 本周要整理的 Clippings

- （待填）

## 周末回顾

- 做得好的事：
- 卡住的事：
- 下周调整：
```

### 4-3. `Templates/知识库页面.md`

```markdown
---
title: 知识库页面
type: knowledge
date: YYYY-MM-DD
tags: []
source:
---

# 知识库页面标题

## 核心概念

用一段话说明这篇笔记在解决什么问题。

## 重点整理

- （待填）

## 可应用场景

- （待填）

## 与既有笔记的连结

- [[相关笔记]]

## 内容缺口

- （待填）
```

### 4-4. Templater 进阶选项

如果使用者熟悉 Obsidian Templater，可以另外建立 Templater 版本模板。

> [!warning]
> 不要把含 `<% tp.xxx %>` 的示范语法放在会被 Templater 自动执行的资料夹。
> 曾遇过教学文件中的 `<% %>` 被误展开，导致范例不可逆地变成日期或具体文字。
> 预设建议先用纯 Markdown 模板。

---

## 阶段五：建立知识库初始档案

### 5-1. `知识库/index.md`

若不存在，建立：

```markdown
---
title: 知识库索引
type: index
updated: YYYY-MM-DD
tags: [知识库]
---

# 知识库索引

> 最后更新：YYYY-MM-DD

## 主题总览

| 主题 | 一行摘要 | 相关笔记 |
|------|----------|----------|
| （等待第一次整理） | | |

## 最近新增

| 日期 | 笔记 | 来源 |
|------|------|------|
| | | |

## 待补缺口

- （待填）
```

### 5-2. `知识库/log.md`

若不存在，建立：

```markdown
---
title: 知识库操作纪录
type: log
tags: [知识库, log]
---

# 知识库操作纪录

## YYYY-MM-DD 初始化

**操作类型：** 初始化

**建立内容：**

- `Clippings/`
- `知识库/`
- `创作库/`
- `每日笔记/`
- `Templates/`
- `AGENTS.md`

**备注：**

- 预设不覆写既有笔记。
```

---

## 阶段六：设定剪藏流程

### 6-1. Web Clipper（可选）

如果使用者有安装 Obsidian Web Clipper：

1. 点浏览器上的 Web Clipper 图示
2. 开启设定
3. 确认 vault 是 `<VAULT_PATH>`
4. Folder / Location 设成 `Clippings`
5. 测试剪一篇文章

### 6-2. 没有 Web Clipper 的替代做法

没有 Web Clipper 也没关系。

使用者可以直接对 Codex 说：

> 帮我把这篇文章存进 Obsidian 的 Clippings，标题是「XXX」，来源网址是「YYY」。

Codex 建议存成：

```markdown
---
title: <文章标题>
source: <网址>
date: YYYY-MM-DD
type: clipping
tags: [Clippings]
---

# <文章标题>

## 原始摘要

## 原文重点

## 我想保留的原因
```

---

## 阶段七：每周知识重整 SOP

Codex App 支援自动化。对一般使用者来说，最稳的主流程是：

1. 先用手动方式跑一次，确认流程正确。
2. 再请 Codex 建立每周自动化。
3. 若自动化失败，仍可回到手动触发。

> [!important]
> 不要把作业系统的 cron / Windows 工作排程器当成预设。那是旧式 CLI 路线，只有进阶使用者需要。

### 7-1. 先手动跑一次（必要验证）

每周日对 Codex 说：

```text
请跑一次我的 Obsidian 每周知识重整。
请依照 vault 的 AGENTS.md 规则执行。
不要覆写既有笔记；若需要更新既有页面，先摘要你要改哪里再询问。
```

手动成功后，确认：

- Codex 能读取 vault
- Codex 能建立周报笔记
- `知识库/index.md` 与 `知识库/log.md` 的更新规则合理
- 没有覆写既有重要笔记

### 7-2. 建立 Codex 自动化（推荐）

手动验证成功后，对 Codex 说：

```text
请帮我建立一个每周日早上 9:17 执行的 Codex 自动化。

自动化名称：Obsidian 每周知识重整

工作区：<VAULT_PATH>

任务内容：
请执行 Obsidian 笔记库的每周知识重整。
请依照 vault 根目录 AGENTS.md 的规则执行。
不要覆写既有笔记。
若需要更新既有页面，先整理更新计划。
完成后建立周报到 `每日笔记/`，并更新 `知识库/index.md` 与 `知识库/log.md`。
最后回报新增与修改的档案清单。
```

Codex 会使用自己的自动化功能建立周期任务。这比叫使用者自己写 cron 或 Windows 工作排程器更适合一般测试者。

### 7-3. AGENTS.md 主动提醒（备援）

可以在全域 `~/.codex/AGENTS.md` 加：

```markdown
## 知识重整提醒

如果今天是周日，且本周尚未做过知识重整，请在对话开始时提醒使用者：
「今天要不要跑一次 Obsidian 每周知识重整？」

只有在使用者同意后才执行。
```

这不是自动化，只是提醒规则；真正定期执行请用 Codex 自动化。

### 7-4. 重整 prompt

```text
请执行 Obsidian 笔记库的每周知识重整。

前提：
- 使用 vault 的 AGENTS.md 规则
- 不覆写既有笔记
- 新增或修改前先说明计划

第一步：盘点本周变动
- 找过去 7 天新增或修改的笔记
- 分成 Clippings、创作库、知识库、每日笔记

第二步：整理 Clippings
- 读本周新增 Clippings
- 分析主题与关联
- 为每个主题建立或更新知识库页面
- 没有新 Clippings 就跳过

第三步：创作库回流
- 读本周创作库变动
- 若有可沉淀的概念，整理到知识库

第四步：知识库健康检查
- 检查孤儿页面、缺少摘要、缺少来源、缺少双向连结

第五步：更新索引与 log
- 更新 `知识库/index.md`
- 在 `知识库/log.md` 追加本次操作纪录

第六步：产出周报
- 建立 `每日笔记/YYYY-WXX 知识重整.md`
- 摘要本周新增知识、可用素材、待补缺口

完成后回报新增与修改的档案清单。
```

### 7-5. CLI 排程是什么？为什么不当主流程？

CLI 排程是指：

- 用 macOS / Linux 的 `cron`，或 Windows 工作排程器
- 在固定时间呼叫 `codex` CLI
- 让作业系统定期启动 Codex 指令

这不是 Codex Desktop 的主流程，也不是一般使用者需要理解的设定。

> [!warning]
> 只有在使用者明确说「我要用终端机 / CLI / 系统排程」时，才补充 CLI 排程。
> 对外懒人包预设请使用 Codex 自动化。

---

## 阶段八：欢迎笔记与验证

### 8-1. 建立欢迎笔记

若不存在，建立：

```markdown
---
title: 欢迎来到你的第二大脑
type: guide
date: YYYY-MM-DD
tags: [Secondbrain, guide]
---

# 欢迎来到你的第二大脑

这个 Obsidian vault 用来收集、整理、沉淀与输出你的知识。

## 你的资料夹

| 资料夹 | 用途 |
|--------|------|
| `Clippings/` | 收集外部材料 |
| `知识库/` | 整理后的知识 |
| `创作库/` | 自己的作品 |
| `每日笔记/` | 每日与每周纪录 |
| `Templates/` | 笔记模板 |

## 你平常只要做两件事

1. 把值得保留的资料放进 `Clippings/`
2. 每周跑一次知识重整

## 可以对 Codex 说

- 搜寻我的笔记有没有跟某主题相关的内容
- 帮我把这篇文章存进 Clippings
- 把这批 Clippings 整理成知识库页面
- 跑一次每周知识重整
```

### 8-2. 验证清单

- [ ] `<VAULT_PATH>` 已确认
- [ ] `Clippings/`、`知识库/`、`创作库/`、`每日笔记/`、`Templates/` 已存在
- [ ] 全域 `~/.codex/AGENTS.md` 已记录 vault 路径
- [ ] `<VAULT_PATH>/AGENTS.md` 已建立
- [ ] 三个纯 Markdown 模板已建立
- [ ] `知识库/index.md` 与 `知识库/log.md` 已建立
- [ ] 欢迎笔记已建立
- [ ] 已完成一次读写测试
- [ ] 已手动跑过一次知识重整
- [ ] 若需要定期执行，已建立 Codex 自动化

---

## 如果失败，照这个顺序检查

1. `<VAULT_PATH>` 是否正确
2. Codex 是否有 vault 写入权限，或 #03 的 MCP 是否已载入
3. 目标档案是否已存在
4. 是否不小心用 Templater 执行了教学范例
5. 是否把全域 AGENTS.md 与 vault 根 AGENTS.md 混在一起

---

## 实测纪录与踩坑笔记

### 踩坑 1：不要用「参考 Claude Code 版」代替完整内容

对外懒人包应该可以独立执行。若写「内容跟 Claude Code 版相同」，测试者会跳到另一个 repo，且可能把 `CLAUDE.md`、Skill、Claude 排程概念混进 Codex。

解法：Codex 版直接内建完整模板与完整步骤。

### 踩坑 2：Templater 不应作为预设

Templater 是 Obsidian 外挂，不是每个使用者都有装。含 `<% tp.xxx %>` 的教学文件也可能被误执行。

解法：预设纯 Markdown；Templater 只放进阶选项。

### 踩坑 3：不要把 CLI 排程当成 Codex 自动化

CLI 排程是用作业系统去呼叫 `codex` CLI，例如 cron 或 Windows 工作排程器。这跟 Codex App 内建自动化不是同一件事。

解法：公开懒人包以 Codex 自动化为推荐路线；手动触发作为验证与备援；CLI 排程只放进阶说明。

### 踩坑 4：不要假设 Google Drive 路径

使用者可能用 OneDrive、Obsidian Sync、iCloud、本机资料夹。

解法：全篇使用 `<VAULT_PATH>`，由 #03 先定位。

---

## 本懒人包不做的事

- 不强迫使用 Google Drive
- 不要求安装 Templater
- 不把 CLI 排程当作预设自动化方案
- 不覆写既有笔记
- 不沿用 Claude Code 版的 `CLAUDE.md`、Skill、Slash command 设计

---

## 相关连结

- [03 连接 Obsidian 第二大脑](03-建立第二大脑-Obsidian.md)
- [Codex AGENTS.md 官方文件](https://developers.openai.com/codex/guides/agents-md)
- [Obsidian Web Clipper](https://obsidian.md/clipper)
