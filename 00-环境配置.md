# Codex 懒人包 #00：环境建置

> 版本：v0.3（Codex 版）
> 更新日期：2026-05-19

> ✅ 本懒人包以 **Codex Desktop app（macOS/Windows）** 为主轴；同时相容 IDE 扩充与 CLI。

---

## 这个懒人包会帮你做什么？

把使用 Codex 懒人包所需的环境一次建置完成：
- 安装 **Codex Desktop app**
- 安装 Git、GitHub CLI、Node.js、uv
- 登入 OpenAI（ChatGPT 帐号或 API Key）
- 验证一切正常

> 💡 **本懒人包只负责安装工具，不含 GitHub 帐号登入。** 需要连接 GitHub、把教材推上线，请接著执行 **懒人包 #02：连接 GitHub**（内含帐号注册引导，没有 GitHub 帐号也能从那里开始）。

---

## 先备条件

- [ ] OpenAI 帐号（ChatGPT Plus/Pro/Business/Edu 或 API 额度）
- [ ] 电脑有网路连线

---

## 请 Codex 帮我执行以下步骤

> ⚠️ 把这份 MD 档的内容贴给 Codex Desktop 对话框，它会自动执行。
> 遇到需要手动操作（🖐️）的地方会暂停指示你。

---

### 步骤零：环境检查

请 Codex 执行以下检查并把缺少的记下来：

1. **作业系统**：Windows / macOS / Linux（Linux 没有 Desktop app，请走 IDE 扩充或 CLI）
2. **网路连线**
3. **Node.js**（后续 MCP 工具需要）：`node --version`（需 18+）
4. **Git**：`git --version`
5. **GitHub CLI**：`gh --version`
6. **uv**（Python 工具管理器）：`uv --version`

---

### 步骤一：安装 Codex Desktop app

🖐️ **手动操作**：

1. 到 [OpenAI Codex 官方下载页](https://developers.openai.com/codex/app)
2. 选 macOS（Intel 或 Apple Silicon）或 Windows 安装档
3. 安装完成、开启 app
4. **Sign in with ChatGPT**（建议，OAuth 自动处理；ChatGPT Plus/Pro 等订阅会自动让 Codex 用）
   - 也可以选 Sign in with API Key，贴 `sk-...` 开头的 OpenAI API Key
5. 登入完看到 Codex 主画面 = OK

> 💡 **不在 Mac/Win**？改装 IDE 扩充：VSCode / Cursor / Windsurf 的 Marketplace 搜「ChatGPT」（OpenAI 官方）；或 CLI：`npm install -g @openai/codex`。三者设定共用，后面所有懒人包都通用。

---

### 步骤二：安装 Node.js（如果未安装或版本 < 18）

> 💡 不是 Codex 本体要的（Desktop app 自带执行环境），但**几乎所有 MCP server 都需要 Node.js**（NotebookLM、Obsidian、Supabase、Firebase 等）。

**Windows**：
```bash
winget install --id OpenJS.NodeJS --accept-source-agreements --accept-package-agreements
```

**macOS**：
```bash
brew install node
```

**Linux（Ubuntu/Debian）**：
```bash
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs
```

确认：`node --version`

---

### 步骤三：安装 Git（如果未安装）

**Windows**：
```bash
winget install --id Git.Git --accept-source-agreements --accept-package-agreements
```

**macOS**：`xcode-select --install`

**Linux**：`sudo apt update && sudo apt install git -y`

确认：`git --version`

---

### 步骤四：安装 GitHub CLI（如果未安装）

**Windows**：
```bash
winget install --id GitHub.cli --accept-source-agreements --accept-package-agreements
```

**macOS**：`brew install gh`

**Linux**：见 [GitHub CLI 官方安装指引](https://github.com/cli/cli/blob/trunk/docs/install_linux.md)

确认：`gh --version`

---

### 步骤五：安装 uv（如果未安装）

uv 是 Python 套件管理工具，后续安装 NotebookLM 等 MCP 工具会用到。

**Windows（PowerShell）**：
```bash
powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"
```

**macOS / Linux**：
```bash
curl -LsSf https://astral.sh/uv/install.sh | sh
```

确认：`uv --version`

---

### 步骤六：最终验证

```bash
git --version
gh --version
node --version
uv --version
```

并回 Codex Desktop 主画面，确认 app 显示已登入、可正常对话。

> ✅ 「环境建置完成！你的电脑已经准备好使用所有 Codex 懒人包了。」

---

## 常见问题

| 问题 | 解法 |
|------|------|
| Desktop app 开不起来 | macOS：右键→开启（首次绕 Gatekeeper）；Win：以系统管理员身分执行一次 |
| Sign in with ChatGPT 卡住 | 改用 Sign in with API Key；或检查浏览器是否封锁 OAuth 跳转 |
| Linux 没有 Desktop app | 改用 VSCode 扩充（搜「ChatGPT」OpenAI 官方）或 CLI（`npm i -g @openai/codex`） |
| winget 找不到 | Windows 10 1809+ 内建；或到 Microsoft Store 安装「App Installer」 |
| Node.js 版本太旧 | 移除旧版重装最新 LTS |
| 公司电脑装不了 winget / brew | Codex Desktop app 安装档是直接 `.dmg`/`.exe`，不需套件管理器 |

---

## 相关连结

- [OpenAI Codex 官方下载](https://developers.openai.com/codex/app)
- [Codex IDE 扩充](https://developers.openai.com/codex/ide)
- [Codex CLI](https://developers.openai.com/codex/cli)
